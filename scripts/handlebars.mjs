import { actionLabel, actionTooltip, lockTooltip, modTitle } from "./helpers.mjs";

export const helpers = {
  "ftc-actionLabel": actionLabel,
  "ftc-actionTooltip": actionTooltip,
  "ftc-lockTooltip": lockTooltip,
  "ftc-modTitle": modTitle,
};
